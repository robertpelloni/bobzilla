# TODO: bobzilla

## Immediate Action Items
- [ ] Initialize `mozilla-unified` submodule via `scripts/fetch-source.sh`.
- [ ] Verify `bobzilla.cfg` is properly loaded during runtime (requires full build).
- [x] Draft a patch for the MicroVM hardware isolation layer in Gecko (`patches/javasandbox-hw-isolation.patch`) to enforce memory boundaries for secure Java execution.
