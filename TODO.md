# TODO: bobzilla

## Immediate Action Items
- [ ] Initialize `mozilla-unified` submodule via `scripts/fetch-source.sh`.
- [ ] Verify `bobzilla.cfg` is properly loaded during runtime (requires full build).
- [x] Draft mock patches for disabling Safe Browsing Google API calls (`patches/privacy/disable-safebrowsing.patch`).
- [x] Scaffold the Javasandbox custom Alpine Linux guest OS image build scripts (`guest-os/`).
