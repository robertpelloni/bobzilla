# TODO: bobzilla

## Immediate Action Items
- [ ] Initialize `mozilla-unified` submodule via `scripts/fetch-source.sh`.
- [ ] Verify `bobzilla.cfg` is properly loaded during runtime (requires full build).
- [x] Draft mock patches for WebGL/WebGPU passthrough mapping in the Javasandbox architecture (`patches/performance/webgpu-passthrough.patch`).
- [x] Draft mock patches for customizing the main browser layout CSS (`patches/ui/custom-layout.patch`).
