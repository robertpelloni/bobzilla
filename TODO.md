# TODO: bobzilla

## Immediate Action Items
- [ ] Initialize `mozilla-unified` submodule via `scripts/fetch-source.sh`.
- [ ] Verify `bobzilla.cfg` is properly loaded during runtime (requires full build).
- [x] Draft mock patches for the Wayland framebuffer bridging into Firefox tabs (`patches/ui/javasandbox-wayland-bridge.patch`).
- [x] Implement actual Firecracker MicroVM API execution commands in `runner-daemon/src/microvm.rs`.
